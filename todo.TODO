[] Create a user with three things -> ID, PreferredTimings, AvailableTimings in database
[] When user made a ENQUIRY -> {USER, PROPERTY}
[] Save ENQUIRY with {USER, PROPERTY}
[] based on analysis of {USER} decide [CALLING] | [SMS] | [EMAIL]
    if the current time is in the slot of user's available timings, Just CALL them if CALLING validation passed