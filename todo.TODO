[done] Create a user with three things -> ID, PreferredTimings, AvailableTimings in database
[done] When user made a ENQUIRY -> {USER, PROPERTY}
[done] Save ENQUIRY with {USER, PROPERTY}
[done] based on analysis of {USER} decide [CALLING] | [SMS] | [EMAIL]
    if the current time is in the slot of user's available timings, Just CALL them if CALLING validation passed

### More features -
[] Create a web-socket server in enquiry-service to update the client in real time for scheduled tasks.
[] Create UI design for showing logs and scheduled tasks on frontend